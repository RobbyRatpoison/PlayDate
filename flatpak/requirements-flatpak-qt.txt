# Same as requirements-flatpak.txt (the GTK build) -- PyQt6/PyQt6-WebEngine/
# QtWebEngine itself comes from the com.riverbankcomputing.PyQt.BaseApp base
# this manifest is built against, not from pip. Keep in sync with
# requirements-flatpak.txt when dependencies change.
#
# QtPy IS still needed from pip, though: it's a separate binding-abstraction
# shim (not part of Riverbank's PyQt/PyQtWebEngine) that pywebview's own Qt
# backend (webview/platforms/qt.py) imports through directly -- confirmed
# live, main.py crashed with "No module named 'qtpy'" without this even
# though PyQt6 itself was already present from the BaseApp.
QtPy==2.4.3

Flask==3.1.3
Werkzeug==3.1.6
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.3.1
waitress==3.0.2

pywebview==6.1

requests==2.33.1
beautifulsoup4==4.14.3
howlongtobeatpy==1.0.23
aiohttp==3.14.3
fake-useragent==2.2.0
soupsieve==2.9.1
urllib3==2.7.0
certifi==2026.2.25
charset-normalizer==3.4.4
idna==3.15

Pillow==12.3.0

RapidFuzz==3.14.3
python-dotenv==1.2.2
watchdog==6.0.0
vdf==3.4
# Ubisoft Connect plugin (config_parser.py) -- see note in ../requirements.txt
PyYAML==6.0.3
