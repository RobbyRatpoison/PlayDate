# PlayDate — Python dependencies
# Generated manually from actual imports. Do NOT replace with pip freeze output.

# Web framework & server
Flask==3.1.3
Werkzeug==3.1.6
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.3.1
waitress==3.0.2

# Desktop window
pywebview==6.1
# pythonnet is a Windows-only transitive dependency of pywebview.
# Pinned to rc0 for Python 3.14 support; update when stable 3.1.0 releases.
pythonnet==3.1.0rc0

# HTTP & scraping
requests==2.33.1
beautifulsoup4==4.14.3
howlongtobeatpy==1.0.21
aiohttp==3.14.3
aiounittest==1.5.0
fake-useragent==2.2.0
soupsieve==2.9.1
urllib3==2.7.0
certifi==2026.2.25
charset-normalizer==3.4.4
idna==3.15

# Image processing
Pillow==12.3.0

# Utilities
RapidFuzz==3.14.3
python-dotenv==1.2.2
watchdog==6.0.0
vdf==3.4
# Used only by the Ubisoft Connect plugin (config_parser.py) to decode the
# YAML game descriptors in Ubisoft Connect's local catalog cache. Kept here
# because plugins have no per-plugin dependency mechanism -- a core grep for
# `import yaml` won't find it. Don't drop it in a "manually from imports" pass.
PyYAML==6.0.3
