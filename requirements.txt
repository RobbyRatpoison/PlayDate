# PlayDate — Python dependencies
# Generated manually from actual imports. Do NOT replace with pip freeze output.

# Web framework & server
Flask==3.1.3
Werkzeug==3.1.6
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.3.1
waitress==3.0.2

# Desktop window
pywebview==6.1
# pythonnet is a Windows-only transitive dependency of pywebview.
# Pinned to rc0 for Python 3.14 support; update when stable 3.1.0 releases.
pythonnet==3.1.0rc0

# HTTP & scraping
requests==2.32.5
beautifulsoup4==4.14.3
soupsieve==2.8.3
urllib3==2.6.3
certifi==2026.2.25
charset-normalizer==3.4.4
idna==3.11

# Browser automation (BLAEO sync only — requires Chrome)
selenium==4.41.0
webdriver-manager==4.0.2

# Image processing
Pillow==12.1.1

# Utilities
RapidFuzz==3.14.3
python-dotenv==1.2.2
watchdog==6.0.0
vdf==3.4
