# Release Notes

## v1.9.4

### Fixes

- Several error messages (API-key test, update and notification checks, plugin status) no longer include raw exception text.
- EA App classic titles uninstalled through EA App are now detected as uninstalled instead of staying marked installed (needs EA App plugin v1.1.2).
